# Changelog

All notable changes to the "Totonoe Log" extension will be documented in
this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

- Initial project scaffolding: extension skeleton, build/test tooling, CI.
- Fix: declare an explicit `onCommand:` activation event for the placeholder
  command, use `npx --no-install` for deterministic packaging checks in CI,
  and remove a duplicate build step in CI.

[Unreleased]: https://github.com/upu/Totonoe-Log/compare/v0.0.1...HEAD
